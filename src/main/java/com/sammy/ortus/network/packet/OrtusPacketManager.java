package com.sammy.ortus.network.packet;

import net.minecraft.resources.ResourceLocation;

import java.util.HashMap;

public class OrtusPacketManager {
    public static final HashMap<ResourceLocation,OrtusPacket> PACKET_TYPES = new HashMap<>();

    /**
     * The method to register all packet types to the hashmap
     */
    public static void registerTypes(){

        //Register them to the network system

    }
}
